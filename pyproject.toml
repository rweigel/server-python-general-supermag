[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "supermag-hapi"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
  "supermag @ git+https://github.com/rweigel/supermag"
]
